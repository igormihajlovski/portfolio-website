<aside
  class="analytics-consent"
  data-analytics-consent
  id="analytics-consent"
  role="region"
  aria-labelledby="analytics-consent-title"
  aria-describedby="analytics-consent-description"
  hidden
>
  <div class="analytics-consent-copy">
    <h2 id="analytics-consent-title">{{ 'consent.title' | t(lang) | escape }}</h2>
    <p id="analytics-consent-description">
      {{ 'consent.description' | t(lang) | escape }}
    </p>
    <a href="/privacy-policy/">{{ 'footer.privacyPolicy' | t(lang) | escape }}</a>
  </div>
  <div class="analytics-consent-actions">
    <button type="button" class="analytics-consent-button analytics-consent-button-secondary" data-consent-choice="necessary">
      {{ 'consent.necessaryOnly' | t(lang) | escape }}
    </button>
    <button type="button" class="analytics-consent-button analytics-consent-button-primary" data-consent-choice="accepted">
      {{ 'consent.acceptAnalytics' | t(lang) | escape }}
    </button>
  </div>
</aside>
